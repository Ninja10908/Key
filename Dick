local b = game:GetService("RunService")
local c = game:GetService("Players").LocalPlayer

local d = Instance.new("ScreenGui")
d.Name = "SpeedGui"
d.ResetOnSpawn = false
d.Parent = c:WaitForChild("PlayerGui")

local e = Instance.new("TextButton")
e.Name = "SpeedButton"
e.Text = "Speed"
e.Size = UDim2.new(0, 60, 0, 30)
e.Position = UDim2.new(1, -95, 0.5, -55)
e.AnchorPoint = Vector2.new(1, 0.5)
e.BackgroundColor3 = Color3.new(0, 0, 0)
e.TextColor3 = Color3.new(1, 1, 1)
e.Font = Enum.Font.GothamBold
e.TextSize = 14
e.Parent = d
Instance.new("UICorner", e).CornerRadius = UDim.new(0, 6)

local f = false
local speed = 100
local h = {"Flying Carpet", "Cupid's Wings", "Santa's Sleigh", "Witch's Broom", "Speed Coil", "Gravity Coil", "Coil Combo", "Invisibility Cloak", "Laser Cape"}
local i, j, k
local l
local toolCheckConnection = nil

local function m()
    if not c.Character then
        c.CharacterAdded:Wait()
    end
    i = c.Character
    j = i:WaitForChild("Humanoid")
    k = i:WaitForChild("HumanoidRootPart")
end

local function unequipAll()
    if j then
        j:UnequipTools()
    end
    for _, tool in ipairs(i:GetChildren()) do
        if tool:IsA("Tool") then
            tool.Parent = c.Backpack
        end
    end
end

local function n()
    for o, p in ipairs(h) do
        local q = i:FindFirstChild(p) or c.Backpack:FindFirstChild(p)
        if q and q.Parent ~= i then
            q.Parent = i
            break
        end
    end
end

local function r()
    if l then
        l:Disconnect()
    end
    l = b.RenderStepped:Connect(function()
        if f and k then
            local s = j.MoveDirection
            if s.Magnitude > 0 then
                k.Velocity = Vector3.new(s.X * speed, k.Velocity.Y, s.Z * speed)
            end
        end
    end)
end

local function blockUnequip()
    if toolCheckConnection then
        toolCheckConnection:Disconnect()
    end
    toolCheckConnection = j:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
        if f then
            j.WalkSpeed = 16
        end
    end)
end

e.MouseButton1Click:Connect(function()
    f = not f
    e.BackgroundColor3 = f and Color3.new(0, 1, 0) or Color3.new(0, 0, 0)
    if f then
        m()
        unequipAll()
        n()
        r()
        blockUnequip()
    else
        if l then
            l:Disconnect()
            l = nil
        end
        if toolCheckConnection then
            toolCheckConnection:Disconnect()
            toolCheckConnection = nil
        end
        j.WalkSpeed = 16
    end
end)

c.CharacterAdded:Connect(function(t)
    task.wait(0.5)
    i = t
    j = t:WaitForChild("Humanoid")
    k = t:WaitForChild("HumanoidRootPart")
    if f then
        unequipAll()
        n()
        r()
        blockUnequip()
    end
end)

getfenv().shsp = function()
    if d then d:Destroy() end
    if l then l:Disconnect() end
    if toolCheckConnection then toolCheckConnection:Disconnect() end
    if j then j.WalkSpeed = 16 end
end
